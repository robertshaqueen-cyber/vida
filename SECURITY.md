# Security Policy

## ⚠️ 项目阶段声明

**vida 处于早期开发阶段，代码未经第三方安全审计。**

在完成独立安全审计之前，**不建议**使用本软件存放生产环境关键凭证
（如云厂商 root 密钥、公司内网生产主机口令）。请自行评估风险。

## 支持的版本

| 版本 | 安全修复支持 |
|---|---|
| 最新 main 分支 | ✅ 尽力修复 |
| 已发布 release | ✅ 尽力修复 |
| 更早版本 | ❌ 不提供 |

## 漏洞上报

**请勿使用公开 Issue 上报安全漏洞。**

请通过 [GitHub Security Advisory](https://github.com/robertshaqueen-cyber/vida/security/advisories/new)
的 **Private vulnerability reporting** 功能上报。

上报时请包含：

1. 漏洞类型与影响范围（例如：越权访问、信息泄露、加密缺陷）
2. 复现步骤（尽量最小化）
3. 受影响的版本 / commit
4. 建议的修复方案（如有）

我们会尽量在 7 天内确认，并尽快在修复后公开披露。

## 威胁模型（Threat Model）

### 保护什么（设计目标）

- **文件被拷贝走**：金库使用 age + scrypt 加密，标准 age CLI 可解密，
  无口令无法读取任何内容。
- **云盘服务商**：即使同步文件夹被服务商拿到，密文不可读。
- **口令缓存**：仅在用户选择记住时长时写入系统钥匙串（Keychain），
  有效期最长 7 天；过期、旧版无期限缓存或主动锁定都会删除该项。

### 不保护什么（已知限制）

- **已解锁状态**：解锁后金库内容（含主机数据与口令）驻留内存，
  具备本机权限的进程理论上有能力读取（与所有同类工具一致）。
- **主口令丢失无法找回**：主口令丢失 = 数据永久丢失，无后门、无恢复通道。
- **本机恶意软件**：如果攻击者已获得你的用户级权限，可读取解锁后的内存。
- **单层加密**：当前整个金库（主机名称、地址、用户名、口令等全部字段）
  是一个整体加密的 age 文件。金库文件在磁盘上是加密的；
  只有解锁后这些字段才以明文出现在内存中。
  将来若实施 hosts.json 与凭据分离存储（M1.5），此说明将更新。
- **输入法（IME）**：口令输入框已禁用 IME，按键直接进入应用，
  不经过输入法缓冲区。副作用：含非拉丁字符的口令需通过粘贴输入。
  （历史问题追踪：https://github.com/robertshaqueen-cyber/vida/issues/2）

### 保护措施（具体做法）

- 密码与私钥内容**永不**写入日志。
- 敏感字段使用 zeroize 在 drop 时清零。
- 金库写入采用原子写入 + fsync，避免半写状态。
- `daemon.token`（WebSocket 认证令牌）权限为 0600，仅限本机读取。
- M4 的 `session-host.sock` 位于权限 0700 的 Vida 配置目录中，socket 本身权限为
  0600。SSH 凭据只通过该本机 IPC 发送给独立会话宿主，不进入子进程 argv、环境变量
  或日志。会话宿主在 daemon 意外退出后会继续持有活动 PTY/SSH；主动锁定会关闭这些
  会话，关闭最后一个会话并退出 daemon 后宿主自行结束。
- **终端会话能力**：daemon 可启动本地 shell 进程（`OpenLocalSession`
  固定使用 `$SHELL`，不接受客户端指定命令）。因此 `daemon.token`
  保护的不再只是金库，**还包括本机 shell 执行能力**——持有 token
  即可通过 WebSocket 在会话中执行任意 shell 命令。token 必须严格
  保密，泄露等于本机 shell 访问权泄露。
- **M5a 只读 CLI**：`vidactl` 当前只调用状态、主机摘要、会话清单和读屏接口，
  不调用 `RevealCredential`，也不提供终端输入或金库修改命令。但这只是产品接口边界，
  不是对同一操作系统用户的安全沙箱；持有 `daemon.token` 的本机进程仍具备上述原始
  daemon 能力。M5 写入策略必须在 daemon 侧实施，不能只依赖 CLI/MCP 隐藏命令。
